{
  "name": "chat-widget",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:example": "tsc && vite build",
    "build:prod": "tsc && vite build --mode production",
    "preview": "vite preview",
    "example": "pnpm build:example && vite preview --outDir example --open"
  },
  "dependencies": {
    "lit": "^3.2.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "typescript": "~5.7.2",
    "vite": "^6.3.0"
  }
}